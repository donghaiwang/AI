# auto add, commit, push to default remote

# add path to user path
# vi ~/.bashrc
# export PATH=$PATH:/home/d/workspace/AI/tools/shell
# source ~/.bashrc
git add .
git commit -m 'auto commit'
git push
